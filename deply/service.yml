apiVersion: v1
kind: Service
metadata:
  name: poc-http
  namespace: default
spec:
  selector:
    app: poc
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
  clusterIP: None
